function add_all() {
    var fridge_large = document.getElementsByTagName("input")[0].value;
    fridge_large = fridge_large * 2.0;
    
    document.getElementById('amount').innerHTML) = fridge_large;
}